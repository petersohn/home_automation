
include_rules

include $(COMPILE_TUP)

: foreach *.cpp |> !cxx |>

include $(LINK_TUP)

: ./test.o | $(CORE_A) |> !linker |> test.elf

include $(FLASH_TUP)

: test.elf |> !elf2bin |> test.bin
: test.bin |> !flash |>
